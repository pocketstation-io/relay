# Phase 1 Signaling Ownership

v2.3 requires signaling message definitions to live inside the relay repo until Phase 2.

Do not extract `pocketstation-io/protocol` until a second SDK consumer needs stable generated types.
