# Benchmark Evidence

Historical benchmark baseline results. These are CI run outputs, not source.

Each file documents:
- command that produced it
- date
- platform
- commit

Files here are not automatically current proof — run `cargo bench` for current numbers.
